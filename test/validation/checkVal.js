// checks the validation
// var Joi = require('joi');
//
// module.exports = {
//   body: {
//     email: Joi.string().email().required(),
//     password: Joi.string().regex(/[a-zA-Z]/).required()
//   }
// };
